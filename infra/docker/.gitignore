# 환경 변수 파일
.env

# Airflow 디렉토리
airflow/logs/
airflow/dags/__pycache__/
airflow/plugins/__pycache__/
airflow/.airflow_db_initialized

# MariaDB 초기화 스크립트 (선택적으로 필요에 따라 주석 해제)
# mariadb/init/*.sql

# 데이터베이스 데이터 (로컬 볼륨 사용 시)
data/

# Docker 임시 파일
*.log
.DS_Store
